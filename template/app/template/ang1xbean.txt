angular.module('app',[])
		.controller('${name}', ${name});
		function ${name}($scope) {
			${method}
                $scope.initialize=function() {
			${prop}	
			}        
                $scope.initialize();                       
		}