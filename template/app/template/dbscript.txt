create table ${table} (
<#list fields as field>
  <#if field.id>
    ${field.colname} int generated by default as identity (start with 1),
  <#elseif !field.obj>
  ${field.colname} ${field.dbtype},
  </#if>
</#list>
 primary key (${primary})
);




